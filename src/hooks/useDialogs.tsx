/**
 * Re-export dialog hooks for backward compatibility
 * @deprecated Import directly from useNotification or useConfirmDialog instead
 */
export { useNotification } from "./useNotification";
export { useConfirmDialog } from "./useConfirmDialog";
